# Tesco Creative Studio - Environment Configuration Template
# Copy this file to .env and fill in your actual values

# ⚠️ SECURITY WARNING:
# - NEVER commit .env file to version control
# - Add .env to .gitignore immediately
# - For production, use environment variables or secret managers:
#   * AWS Secrets Manager
#   * Azure Key Vault  
#   * Google Secret Manager
#   * HashiCorp Vault

# Google Gemini API Key (REQUIRED for AI features)
# Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Application Settings
APP_NAME=Tesco Creative Studio
APP_VERSION=1.0.0
DEBUG=True

# File Storage Directories
UPLOAD_DIR=./uploads
EXPORT_DIR=./exports
MAX_FILE_SIZE=10485760

# CORS Origins (comma-separated, no spaces)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173

# Allowed File Extensions (comma-separated, no spaces)
ALLOWED_EXTENSIONS=jpg,jpeg,png,webp,gif,svg,bmp,tiff,tif
